version: '3'
services:
    rasa:
      container_name: "chatbot_green"
      user: root
      build: 
        context: ./rasa
      volumes:
        - "./rasa:/app"
      ports: 
        - "10000:5005"
    action_server:
      container_name: "action_server_green"
      build: 
        context: ./rasa/actions
      volumes:
        - ./rasa:/app/actions
        - ./rasa:/app/data
      ports:
        - 10001:5055