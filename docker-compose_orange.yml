version: '3'
services:
    rasa:
      container_name: "chatbot_orange"
      user: root
      build: 
        context: ./rasa
      volumes:
        - "./rasa:/app"
      ports: 
        - "10020:5005"
    action_server:
      container_name: "action_server_orange"
      build: 
        context: ./rasa/actions
      volumes:
        - ./rasa:/app/actions
        - ./rasa:/app/data
      ports:
        - 10021:5055